<?php

namespace Smolblog\Core\Toolkits;

/**
 * Trait to handle most of the boilerplate of endpoints. Takes over the `run` function and provides a
 * `runParsed` function.
 */
trait EndpointToolkit {
}
