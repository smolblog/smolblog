<?php

namespace Smolblog\Foundation;

/**
 * A service is a class that performs a function or small set of functions. It does not contain state and can function
 * as a singleton.
 *
 * Empty interface right now, mostly just as a signal.
 */
interface Service {
}
